# core
pandas>=2.2
pyarrow>=16.0          # parquet engine used by pandas/geopandas
numpy>=1.26
requests>=2.32
python-dotenv>=1.0
tqdm>=4.66

# geospatial
geopandas>=1.0
shapely>=2.0
rasterio>=1.3          # reads ESA WorldCover COGs over HTTP
pyproj>=3.6
osmium>=4.0            # parses the Geofabrik .osm.pbf extract

# ml
scikit-learn>=1.5      # DBSCAN, BallTree, GroupKFold, metrics
scipy>=1.11            # uniform_filter for land-cover neighbourhoods
lightgbm>=4.5          # classifier; also supplies TreeSHAP via pred_contrib

# api
fastapi>=0.115
uvicorn[standard]>=0.32
